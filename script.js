// You can add JavaScript to handle dynamic behavior or interactions if needed
document.querySelectorAll('.add-button').forEach(button => {
    button.addEventListener('click', () => {
        alert('Item added to cart!');
    });
});
